CREATE TABLE [csb].[Overview] (
    [OverviewID] INT           NOT NULL,
    [GoLiveDate] DATE          NULL,
    [Overview]   VARCHAR (MAX) NOT NULL,
    PRIMARY KEY CLUSTERED ([OverviewID] ASC)
);

